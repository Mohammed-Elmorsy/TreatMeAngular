export class SessionDetails {
    constructor(

       public doctorId :Number,
       public days:String[], 
       public duration:Number,
       public startAM:Number,
       public endAM:Number,
       public startPM:Number,
       public endPM:Number,

    ){

    }
}
